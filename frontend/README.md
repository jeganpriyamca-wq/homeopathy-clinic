# Homeopathy Clinic Frontend

React + TypeScript + Vite frontend.

## Setup

1. Copy `.env.example` to `.env.local`.
2. Set `VITE_API_BASE_URL` to the Codespaces forwarded backend URL.
3. Run:

```bash
npm install
npm run dev
```
